#include "stdafx.h"
//#include <ModeIntro.h>
//#include <ModeMenu.h>

TEST(CheckModeTransitions)
{
	/*
	IMode* mi = new ModeIntro();
	IMode* mm = mi->Teardown();
	CHECK(mm);
	CHECK_EQUAL(ModeType::Menu, mm->GetType());
	*/
}